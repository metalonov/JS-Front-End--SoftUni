function tofixed(num) {
    console.log(num.toFixed(2)); // formats it down to the second decimal
}

function repeat(variable) {
    console.log(variable.repeat(4)); // repeats it 4 times
}

function solve(animals) {
    const animasl = ['ant', 'bison', 'camel', 'duck', 'elephant'];

    console.log(animals.slice(2));
    // Expected output: Array ["camel", "duck", "elephant"]
}